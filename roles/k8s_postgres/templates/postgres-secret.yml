apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: default
type: Opaque
data:
  # Base64 encoded values - Ansible will encode these
  postgres-user: {{ db_username | b64encode }}
  postgres-password: {{ db_password | b64encode }}
  postgres-db: {{ db_name | b64encode }}
