# Database configurations
db_network: dnet
db_host: postgres-db
db_image: postgres:latest
db_name: airtrail
db_username: airtrail_user
db_password: airtrail_secret
# Derive DB URL from components so it follows the configured `db_host`.
# The Postgres Service name used by the role is `{{ db_host }}` (see postgres-service.yml).
db_url: "postgres://{{ db_username }}:{{ db_password }}@{{ db_host }}:5432/{{ db_name }}"

airtrail_image: "luis25franca/airtrail:v3" 
pvc_name: postgres-pvc

# BEGIN ANSIBLE MANAGED AIRTRAIL ENDPOINT
# app endpoint (managed)
app_ip: "34.27.78.157"
app_port: "3000"
# END ANSIBLE MANAGED AIRTRAIL ENDPOINT
